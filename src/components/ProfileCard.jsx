const ProfileCard = () => {
  let styles = {
    backgroundColor: "lightgrey",
    padding: "15px",
    borderRadius: "8px",
    color: "green",
    fontSize: "1.5rem",
  };

  return <div style={styles}>ProfileCard</div>;
};

export default ProfileCard;
